# Fal.ai API key for AI-enhanced renders (get one at https://fal.ai/dashboard/keys)
# Server-side only: never use VITE_ prefix so the key is never sent to the browser.
#
# - Vercel: add FAL_KEY in Project Settings → Environment Variables.
# - Local dev (with `vercel dev`): add FAL_KEY below (no VITE_ prefix).
FAL_KEY=your_fal_key_here
